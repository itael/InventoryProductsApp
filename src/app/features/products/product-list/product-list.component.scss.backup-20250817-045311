@use '../../../shared/styles/variables' as *;
@use '../../../shared/styles/mixins' as *;

// === CONTAINER AND LAYOUT ===
.product-list-container {
  min-height: 100vh;
  background-color: $bg-light;
}

.page-header {
  background: $bg-gradient-primary;
  color: white;
  padding: 30px 0;
  box-shadow: $shadow-md;

  @include mobile { padding: 20px 0; }

  .header-content {
    @include container;
    
    & {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    @include mobile {
      flex-direction: column;
      gap: 20px;
      text-align: center;
    }

    h1 {
      margin: 0;
      @include text-responsive($font-size-2xl, $font-size-xl, $font-size-lg);
    }

    .header-actions {
      display: flex;
      gap: 10px;

      @include mobile {
        justify-content: center;
        width: 100%;
        flex-wrap: wrap;
      }

      .btn {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        @include button-responsive;

        @include mobile {
          flex: 1;
          min-width: 120px;
          justify-content: center;
        }
      }
    }
  }
}

// === SEARCH AND CONTROLS ===
.search-controls {
  background: white;
  padding: 20px 0;
  border-bottom: 1px solid $color-border;
  position: sticky;
  top: 0;
  z-index: 100;

  @include mobile { padding: 15px 0; }

  .controls-content {
    @include container;
    
    & {
      display: flex;
      gap: 15px;
      align-items: center;
      flex-wrap: wrap;
    }

    @include mobile {
      flex-direction: column;
      gap: 15px;
    }

    .search-input {
      flex: 1;
      min-width: 200px;

      @include mobile {
        width: 100%;
        min-width: unset;
      }

      input {
        width: 100%;
        @include button-responsive;

        @include mobile {
          padding: 12px 16px;
          font-size: $font-size-base;
        }
      }
    }

    .view-toggle {
      display: flex;
      gap: 5px;

      @include mobile {
        width: 100%;
        justify-content: center;
      }

      .btn {
        @include button-responsive;
        
        @include mobile {
          flex: 1;
          min-width: 60px;
        }
      }
    }
  }
}

// === LOADING STATE ===
.loading-state {
  @include flex-center;
  @include flex-column;
  padding: 60px 20px;
  gap: 20px;

  @include mobile { padding: 40px 20px; }

  .loading-spinner {
    @include loading-spinner;
  }

  .loading-text {
    @include text-responsive;
    color: $color-text-secondary;
  }
}

// === MAIN CONTENT ===
.products-content {
  @include container;
  padding: 30px 20px;
  width: 100%;
}

// === GRID VIEW ===
.grid-container {
  display: grid;
  gap: 25px;
  width: 100%;
  max-width: 1200px;
  justify-items: center;

  // Responsive grid columns
  grid-template-columns: 1fr;
  
  @include tablet {
    grid-template-columns: repeat(2, 1fr);
  }
  
  @include desktop {
    grid-template-columns: repeat(3, 1fr);
  }
  
  @include desktop-lg {
    grid-template-columns: repeat(4, 1fr);
  }

  @include mobile {
    gap: 20px;
    grid-template-columns: 1fr;
  }
}

// === PRODUCT CARD ===
.product-card {
  background: white;
  border-radius: $border-radius-md;
  box-shadow: $shadow-sm;
  overflow: hidden;
  transition: all $transition-normal;
  width: 100%;
  max-width: 350px;
  min-height: 400px;
  display: flex;
  flex-direction: column;

  &:hover {
    box-shadow: $shadow-md;
    transform: translateY(-2px);
  }

  @include mobile {
    max-width: none;
    min-height: 350px;
  }

  .product-image {
    width: 100%;
    height: 200px;
    background: $bg-light;
    position: relative;
    overflow: hidden;

    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform $transition-normal;
    }

    &:hover img {
      transform: scale(1.05);
    }

    .image-placeholder {
      @include flex-center;
      width: 100%;
      height: 100%;
      background: $bg-light;
      color: $color-text-secondary;
      font-size: $font-size-lg;
    }
  }

  .product-info {
    padding: 20px;
    flex: 1;
    display: flex;
    flex-direction: column;

    @include mobile { padding: 15px; }

    .product-name {
      font-size: $font-size-lg;
      font-weight: $font-weight-semibold;
      color: $color-text;
      margin-bottom: 8px;
      line-height: 1.3;

      @include mobile { font-size: $font-size-base; }
    }

    .product-description {
      color: $color-text-secondary;
      font-size: $font-size-sm;
      line-height: 1.4;
      margin-bottom: 12px;
      flex: 1;
      
      @include mobile { font-size: $font-size-xs; }
    }

    .product-details {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-bottom: 15px;
      font-size: $font-size-sm;

      .detail-row {
        display: flex;
        justify-content: space-between;
        align-items: center;

        .label {
          color: $color-text-secondary;
          font-weight: $font-weight-medium;
        }

        .value {
          color: $color-text;
          font-weight: $font-weight-semibold;
        }
      }

      .price {
        .value { color: $primary; }
      }

      .discount .value {
        color: $success;
      }
    }

    .product-actions {
      border-top: 1px solid $color-border;
      display: flex;
      justify-content: center;
      gap: 8px;
      margin-top: auto;
      padding-top: 15px;

      @include mobile {
        flex-direction: column;
        gap: 10px;
      }

      .btn {
        flex: 1;
        padding: 8px 12px;
        text-decoration: none;
        text-align: center;
        border-radius: $border-radius-sm;
        font-size: $font-size-xs;
        font-weight: $font-weight-medium;
        border: none;
        cursor: pointer;
        transition: all $transition-normal;
        display: inline-flex;
        justify-content: center;
        align-items: center;
        gap: 4px;

        @include mobile {
          width: 100%;
        }

        &:disabled {
          opacity: 0.6;
          cursor: not-allowed;
        }

        // Button variants
        &.btn-info {
          background: $info;
          color: white;
          &:hover:not(:disabled) { background: $info-dark; }
        }

        &.btn-warning {
          background: $warning;
          color: white;
          &:hover:not(:disabled) { background: $warning-dark; }
        }

        &.btn-danger {
          background: $danger;
          color: white;
          &:hover:not(:disabled) { background: $danger-dark; }
        }
      }
    }
  }
}

// === LIST VIEW ===
.list-container {
  display: flex;
  flex-direction: column;
  gap: 15px;
  width: 100%;

  @include mobile { gap: 10px; }
}

.product-list-item {
  background: white;
  border-radius: $border-radius-md;
  box-shadow: $shadow-sm;
  overflow: hidden;
  transition: all $transition-normal;
  display: flex;
  min-height: 120px;

  &:hover {
    box-shadow: $shadow-md;
  }

  @include mobile {
    flex-direction: column;
    min-height: auto;
  }

  .product-image {
    width: 120px;
    height: 120px;
    flex-shrink: 0;
    background: $bg-light;

    @include mobile {
      width: 100%;
      height: 150px;
    }

    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .image-placeholder {
      @include flex-center;
      width: 100%;
      height: 100%;
      background: $bg-light;
      color: $color-text-secondary;
    }
  }

  .product-details {
    flex: 1;
    padding: 20px;
    display: flex;
    justify-content: space-between;

    @include mobile {
      flex-direction: column;
      gap: 15px;
      padding: 15px;
    }

    .product-info {
      flex: 1;

      .product-name {
        font-size: $font-size-lg;
        font-weight: $font-weight-semibold;
        color: $color-text;
        margin-bottom: 8px;
      }

      .product-description {
        color: $color-text-secondary;
        font-size: $font-size-sm;
        margin-bottom: 12px;
        line-height: 1.4;
      }

      .product-meta {
        display: flex;
        gap: 20px;
        font-size: $font-size-sm;

        @include mobile {
          flex-direction: column;
          gap: 8px;
        }

        .meta-item {
          display: flex;
          align-items: center;
          gap: 5px;

          .label {
            color: $color-text-secondary;
            font-weight: $font-weight-medium;
          }

          .value {
            color: $color-text;
            font-weight: $font-weight-semibold;
          }
        }

        .price .value { color: $primary; }
        .discount .value { color: $success; }
      }
    }

    .product-actions {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 8px;
      align-items: flex-end;

      @include mobile {
        flex-direction: row;
        align-items: center;
        justify-content: center;
      }

      .btn {
        min-width: 80px;
        padding: 6px 12px;
        text-decoration: none;
        text-align: center;
        border-radius: $border-radius-sm;
        font-size: $font-size-xs;
        font-weight: $font-weight-medium;
        border: none;
        cursor: pointer;
        transition: all $transition-normal;

        @include mobile {
          flex: 1;
          min-width: 60px;
        }

        &:disabled {
          opacity: 0.6;
          cursor: not-allowed;
        }

        // Button variants (same as grid view)
        &.btn-info {
          background: $info;
          color: white;
          &:hover:not(:disabled) { background: $info-dark; }
        }

        &.btn-warning {
          background: $warning;
          color: white;
          &:hover:not(:disabled) { background: $warning-dark; }
        }

        &.btn-danger {
          background: $danger;
          color: white;
          &:hover:not(:disabled) { background: $danger-dark; }
        }
      }
    }
  }
}

// === EMPTY STATE ===
.empty-state {
  @include flex-center;
  @include flex-column;
  padding: 60px 20px;
  gap: 20px;
  color: $color-text-secondary;
  text-align: center;

  @include mobile { padding: 40px 20px; }

  .empty-icon {
    font-size: 4rem;
    opacity: 0.3;
    margin-bottom: 10px;
  }

  .empty-title {
    font-size: $font-size-xl;
    font-weight: $font-weight-semibold;
    color: $color-text;
    margin-bottom: 8px;
  }

  .empty-message {
    font-size: $font-size-base;
    line-height: 1.5;
    max-width: 400px;
  }

  .empty-action {
    margin-top: 20px;
    
    .btn {
      @include button-responsive;
      background: $primary;
      color: white;

      &:hover {
        background: $primary-dark;
      }
    }
  }
}

// === RESPONSIVE UTILITIES ===
@include mobile {
  .hide-mobile { display: none !important; }
}

@include tablet {
  .hide-tablet { display: none !important; }
}

@include desktop {
  .hide-desktop { display: none !important; }
}
